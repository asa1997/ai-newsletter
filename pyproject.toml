[project]
name = "ai-newsletter"
version = "0.1.0"
description = ""
authors = [
    {name = "Your Name",email = "you@example.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "crewai (>=0.148.0,<0.149.0)"
]

[tool.poetry]
packages = [{include = "ai_newsletter", from = "src"}]

[tool.poetry.dependencies]
python = ">=3.10.0,<3.12"

[tool.poetry.scripts]
ai-newsletter = "ai_newsletter.main:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
